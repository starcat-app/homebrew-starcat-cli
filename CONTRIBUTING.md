# Contributing

The stable Formula is generated by `starcat-app/starcat-cli` release automation. Do not manually change release URLs or checksums without documenting why the automation cannot be used.

Before opening a pull request, run:

```bash
brew style Formula/starcat.rb
brew audit --strict --formula Formula/starcat.rb
```

Never replace a checksum unless it matches an immutable published release asset.
